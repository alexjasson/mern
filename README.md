# pern
A basic PERN stack

## Setup

1. Install dependencies:
```bash
npm install
```

2. Setup .env file:
```bash
mkdir -p config
echo "PORT=3000" > config/.env
echo "HOST=localhost" >> config/.env
```